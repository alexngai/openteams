name: fixer
display_name: Bug Fixer
description: "Applies targeted code fixes based on investigation findings. Writes minimal, focused changes and runs tests to confirm the fix."
capabilities:
  - codebase.read
  - codebase.search
  - file.write
  - exec.run
  - exec.test
