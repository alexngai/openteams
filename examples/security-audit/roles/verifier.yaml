name: verifier
display_name: Remediation Verifier
description: "Confirms that security remediations resolve the original findings without introducing new vulnerabilities or breaking existing functionality."
capabilities:
  - codebase.read
  - codebase.search
  - exec.run
  - exec.test
