name: secrets-scanner
display_name: Secrets Scanner
description: "Scans the codebase and git history for leaked credentials, API keys, tokens, and other sensitive data."
capabilities:
  - codebase.read
  - codebase.search
